@model DeleteReportViewModel

@{
    string modalId = $"modal{Model.ID}";
    string modalIdReference = $"#{modalId}";
}

<div class="modal fade" id="@modalId" role="dialog">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Delete report</h5>
                <button type="button" class="close" data-dismiss="modal">
                    <span>&times;</span>
                </button>
            </div>
            <div class="modal-body">
                Are you sure you want to delete this report?
            </div>
            <div class="modal-footer">                
                <form method="POST" asp-controller="Reports" asp-action="DeleteReport">
                    <input type="hidden" asp-for="@Model.ID" value="@Model.ID" />
                    <input type="hidden" asp-for=@Model.Page value=@Context.Request.Query["page"] />
                    <input type="hidden" asp-for=@Model.Search value=@Context.Request.Query["search"] />
                    <input type="hidden" asp-for=@Model.Order value=@Context.Request.Query["sort"] />
                    <input type="hidden" asp-for=@Model.StatusFilter value=@Context.Request.Query["status"] />

                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>

                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
            </div>
        </div>
    </div>
</div>

<button 
    type="button" 
    class="btn btn-danger ml-3" 
    data-toggle="modal" 
    data-target="@modalIdReference">
    Delete
</button>